# Kubernetes Services Labs

Student: Azeem

This repository contains Kubernetes Service labs:

- `lab1/` – Why Services are needed (ClusterIP in front of nginx pods).
- `lab2/` – Service types: ClusterIP + NodePort, with browser access via port-forward.
